# Luxe Garden — Venue Website Starter (Premium)

A premium starter site for a **garden venue** (weddings, events, dining, kids experiences) built with:
- React + Vite
- TailwindCSS
- React Router

## Run
```bash
npm install
npm run dev
```

## Edit brand details
Update `src/config/brand.js`.

## Notes
- Booking form uses `mailto:` so it works without a backend.
- Replace gallery placeholders with real images later (recommended).
